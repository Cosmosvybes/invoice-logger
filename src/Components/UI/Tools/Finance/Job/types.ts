export default interface JOBINTERFACE {
  jobID: number;
  title: string;
  description: string;
  client: string;
  budget: number;
  JobStatus: number;
  category: string;
  executionDuration: string;
  postedAt: string;
}
